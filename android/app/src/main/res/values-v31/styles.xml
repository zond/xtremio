<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12 and up draw their own splash screen and ignore
     windowBackground, so the brand ground and mark are named again here. The
     system masks the icon to a circle and asks for a 108dp drawable, which is
     exactly the adaptive-icon foreground. -->
<resources>
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowSplashScreenBackground">@color/ic_launcher_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
